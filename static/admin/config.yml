backend:
  name: git-gateway
  branch: main

media_folder: '/static/media'
public_folder: '/media'

collections:
  - name: 'designers'
    media_folder: ''
    public_folder: ''
    label: 'Designers'
    folder: 'content/designers'
    create: true
    slug: "{{slug}}"
    editor:
      preview: false
    fields:
      - { label: 'Name', name: 'title', widget: 'string'}
      - { label: 'Introduction', name: 'introduction', widget: 'string' }
      - { label: 'Cover Image', name: 'thumbnail', widget: 'image'}
      - label: "Portfolio & Social media links"
        hint: "Seperate each item with a comma"
        name: "links"
        widget: "list"
        default: [""]
      - label: "Projects"
        name: "projects"
        widget: "list"
        required: false
        fields:
          - { label: "Title", name: "title", widget: "string"}
          - { label: "Description", name: "description", widget: "string", required: false }
          - { label: "Video", name: "video", widget: "string", hint: 'Must be Vimeo or YouTube URL', required: false }
          - label: "Images"
            hint: "All images should be below 750kb"
            name: "images"
            widget: "list"
            fields:
              - { label: "Image", name: "src", widget: "image" }